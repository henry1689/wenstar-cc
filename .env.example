# 太虚境·文曲星 环境配置
# 复制此文件为 .env 并按需修改

# P1-5: 语义融合（默认关闭，开启后碎片合并减少token）
ENABLE_SEMANTIC_FUSION=false

# P1-1: 黑钻向量相似度阈值（0.5=平衡，0.4=高召回，0.6=高精度）
VECTOR_SIM_THRESHOLD=0.5

# DeepSeek API 密钥（设置后脱离 MockLLM 降级模式）
# DEEPSEEK_API_KEY=sk-your-key-here

