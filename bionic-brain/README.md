# 景幻仙姑 · 生物智脑独立引擎
#
# 太虚境底层知识管理微服务。
# 调用者不知其存在——只看到 Input → Output。
# 景幻仙姑在后台管理：砂金库 → IQC → 金库 → 黑钻IQC → 黑钻库。
#
# 核心原则：
#   1. 无感——所有内部逻辑对调用者完全透明
#   2. 速度——检索 < 200ms，做梦模式移出重计算
#   3. 数据洁癖——三库流转 + 双写保护
#
# 端口：7200（不与 WenStar 3000/3002 冲突）
# 运行：python main.py
