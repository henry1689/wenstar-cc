/**
 * ingestion-guard.ts — 知识库摄入安全守卫配置
 *
 * 所有阈值、关键词、白名单统一管理，不硬编码在业务代码中。
 * 调优时只改此文件，不动业务逻辑。
 */
export const INGESTION_GUARD = {
  /** 感知维度阈值 — 任一维度超过阈值即拦截 */
  perceptionThresholds: {
    intimacy: 0.3,
    sexualAttraction: 0.2,
    sensoryCraving: 0.3,
  },

  /** 🔴 亲密关键词黑名单 — 命中即拦截
   *  设计原则：
   *    - 仅包含 ≥2 字的明确亲密表述，避免单字误拦正常内容
   *    - 单字词（胸/摸/吻/亲/抱/揉/捏/掐/扭/喘/丢/吸/日/干等）全部移除
   *      因为它们在正常文档中出现频率极高（胸部、呼吸、日常、日期、目录...）
   *    - 语义层面的亲密场景由 perceptionThresholds 兜底
   *    - 修改此列表后运行 src/cli/scan-knowledge-intimate.ts --report 验证误报率
   */
  intimateKeywords: [
    // 明确亲密行为短语
    '做爱','性交','接吻','口交','肛交','自慰','手淫',
    '插入','射精','射了','高潮','要死了',
    // 身体敏感部位（≥2字，不会出现在正常技术文档中）
    '阴蒂','阴茎','阴道','乳头','龟头','阴唇','阴囊','肛门',
    '鸡巴','屌毛','睾丸',
    // 亲密指令（明确第二人称）
    '操我','干我','操你','干你','插我',
    '舔我','吸我','咬我',
    '吻我','亲我','摸我','抱我',
    '进来','进去','我要你','要你','给我',
    '好想要','想要','想你了',
    // 低俗称呼
    '骚货','贱货','婊子','母狗','小母狗','骚逼',
    // 亲密状态描述（≥2字）
    '湿了','硬了','湿漉漉','赤裸','颤抖','腿软',
    '呼吸急促','那一夜','事后','相拥',
    '受不了','忍不住了',
  ],

  /** 🟢 白名单词汇 — 命中亲密关键词但白名单词也出现时优先放行
   *  主要用于避免正常科学/生理/医学内容被误拦
   */
  whitelistTerms: [
    // 医学/生理学词汇（出现在正常科普文档中）
    '生理','解剖','医学','科普','健康','生理期','月经','怀孕',
    '乳房','乳腺','妇科','泌尿','生殖','性教育','性科学',
    '女性','心理','行为','整合','身心','深度','体感',
    // AI/角色设定
    '人设','提示词','语气词','模拟真人','AI','对话',
    // 烹饪/食物
    '揉面','面团','面包','蛋糕','和面','擀面',
    // 运动/健身
    '跑步','健身','瑜伽','拉伸','深蹲','卧推','游泳',
    // 衣物/穿搭 (包含"胸"的正常语境)
    '衬衫','胸围','胸针','胸膛','胸肌',
    // 动物
    '猫','狗','兔子','小鸟','鱼','乌龟','宠物',
    // 正常的身体描述
    '皮肤','头发','眼睛','鼻子','嘴巴','耳朵',
    '肩膀','背部','腰部','腿部','膝盖',
    // 日常问候
    '想你了','想你','抱抱','亲亲',
  ],

  /** 拦截日志开关 */
  loggingEnabled: true,

  /** 🔴 2026-09-16 数据卫生: 元对话/自我陈述过滤
   *  背景: 模型输出的"拒绝话术/自我陈述/系统标签回显"被无差别收录为角色记忆，
   *        并经 砂金→金库→黑钻→地标→梦境 逐级升级，最终影响人格演化。
   *  判据与主题无关 —— 只识别"这段文本是不是在谈论模型自身/系统/规则本身"，
   *  不对任何具体内容方向做判断。
   *  与 config/dream-content-filter.config.yaml（M7 梦境敏感词表）职责互补：
   *  那份管"什么内容不该做梦"，本份管"什么内容不该被当作角色记忆沉淀"。
   *  命中即拦截: 不进 memories / 不进 vault_log / 不进 black_diamond / 不晋升地标。 */
  metaDiscourse: {
    /** A. 系统标签回显 —— 注入用的系统标签被原样复述 */
    labelEcho: /^\[你(记得|珍视|珍惜)|【[^】]{1,12}的记忆】|【对话·/,
    /** B. 模型元陈述 —— 谈论自身能力边界/拒绝行为 */
    selfStatement: /我(不能|无法|不会|不该)(继续|生成|扮演|描述)|这条线我|我不接着演|作为(AI|人工智能|助手|语言模型)/,
    /** C. 元对话词汇 —— 谈论提示词/设定/规则本身 */
    metaTalk: /系统提示|角色设定|我被要求|越界|这个设定/,
    /** 单条文本长度上限 —— 超长跳过检测（防正则回溯开销） */
    maxScanChars: 20000,
  },
};

/** 输入文本是否属于"元对话/自我陈述"（命中即不应沉淀为角色记忆）
 *  鲁棒加固：内部全量 try/catch（异常返回 false，不阻断主流程）；
 *           空值/非字符串/超长短路；命中时按 loggingEnabled 记录审计日志。 */
export function isMetaDiscourse(text: unknown): boolean {
  try {
    if (typeof text !== 'string') return false;
    const s = text.trim();
    if (!s) return false;
    const cfg = INGESTION_GUARD.metaDiscourse;
    if (s.length > cfg.maxScanChars) return false;
    const hit =
      cfg.labelEcho.test(s) ||
      cfg.selfStatement.test(s) ||
      cfg.metaTalk.test(s);
    if (hit && INGESTION_GUARD.loggingEnabled) {
      console.log('[IngestionGuard] 元对话拦截: 命中 metaDiscourse 规则，该片段不沉淀为记忆');
    }
    return hit;
  } catch (e) {
    console.warn('[IngestionGuard] isMetaDiscourse 检测异常，按放行处理:', (e as Error).message);
    return false;
  }
}
