# 豁免记录：P1 火车查询正则修复

- 日期：2026-08-25
- 任务：`P1-TRAIN-01`
- 文件：`src/webui/chat.ts`
- 有效范围：复用该文件已由项目所有者签发的限时 `edit` 豁免窗口；不扩大到其他源码文件。
- 原因：用户原有未提交火车查询代码在 `RegExp` 字符串中使用单反斜杠 `\s`，运行时会退化为普通字符 `s`，导致带空格的站名语句匹配失败。

## 修改与验证

- 两处字符串正则改为 `\\s*`，不改变现有站名表、工作流调用或超时策略。
- 新增 `src/webui/chat/__tests__/train-query-regex.test.ts`，覆盖有/无空格、不同站名及中英文标点。
- 定向测试：1 个文件、5 项测试通过。
- 根项目 `tsc --noEmit` 通过。
- 不提交、不推送、不重启服务。
